#include <stdlib.h>
#include <stdio.h>

#include "mtppacket.h"

int main() {
    //opens	a UDP server socket.
    //receives DATA packets from sender
    //creates and sends back ACK packets (type = 0) to the sender
    
    return 0;
}
